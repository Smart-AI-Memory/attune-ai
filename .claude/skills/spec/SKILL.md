---
name: spec
description: "Spec Ladders — goal-driven spec development: brainstorm, plan, review, and execute a gated task ladder with recorded approvals. Triggers on: spec, spec ladders, brainstorm and build, plan and execute, idea to code, build from scratch."
argument-hint: "<what to build, or 'resume'>"
---

# Spec (project shim)

This project shim makes `/spec` available before the next plugin release. The
canonical workflow is [plugin/skills/spec/SKILL.md](../../../plugin/skills/spec/SKILL.md);
read it now and follow it exactly. Its shared command workspace is preferred
when available, while the existing intake and AskUserQuestion paths remain the
fallback. If this shim and the plugin copy disagree, the plugin copy wins.
