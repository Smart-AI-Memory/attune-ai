---
type: error
name: two-parallel-help-template-generators-in-the-attune-ecosystem
confidence: Verified
tags: [security]
source: .claude/CLAUDE.md
---

# Error: Two parallel help-template generators in the attune
  ecosystem drift silently

## Signature

Two parallel help-template generators in the attune
  ecosystem drift silently

## Root Cause

`src/attune/help/generator.py` (attune-ai's built-in) produces only the 3 core depths (concept/task/reference). `attune_author.generator` (attune-author's, invoked by `scripts/regenerate_help.py` or the new `--all-kinds` CLI flag) produces 11 kinds (adds error/faq/note/quickstart/tip/warning/comparison/ troubleshooting). Features generated by the in-repo tool end up with an incomplete surface when the rest of the codebase uses the 11-kind convention. Symptoms: orphan dirs like `.help/templates/security/` with 3 templates and no manifest entry; new features like `rag-grounding` initially limited to 3 kinds until regenerated through attune-author. Fix for a single feature: `attune-author generate <feat> --help-dir .help --project-root . --all-kinds`. Longer-term fix (not taken): delete the in-repo 3-depth generator or make it a thin wrapper around attune-author so there's one source of truth for the kind list.

## Resolution

1. `src/attune/help/generator.py` (attune-ai's built-in) produces only the 3 core depths (concept/task/reference)

## Confidence

**Verified** — Confirmed by prior incident (Lessons Learned)

## Related Topics
- Tip: Best practice: Two parallel help-template generators in the attune
  ecosystem drift silently
