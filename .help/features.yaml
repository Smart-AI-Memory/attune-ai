version: 1

# Top-level narrative docs not owned by any single feature.
# These are hand-written and never regenerated from source.
_docs:
  # Reference material
  - docs/reference/FAQ.md
  - docs/reference/TROUBLESHOOTING.md
  - docs/reference/API_REFERENCE.md
  - docs/reference/glossary.md
  - docs/reference/empathy-os.md
  - docs/reference/core.md
  - docs/reference/config.md
  - docs/reference/index.md
  # Cross-cutting how-to narratives
  - docs/how-to/practical-patterns.md
  - docs/how-to/prerequisites.md
  - docs/how-to/index.md
  # Tutorials
  - docs/tutorials/installation.md
  - docs/tutorials/build-a-workflow.md
  - docs/tutorials/index.md
  - docs/tutorials/examples/adaptive-learning-system.md
  - docs/tutorials/examples/multi-agent-team-coordination.md
  - docs/tutorials/examples/sbar-clinical-handoff.md
  - docs/tutorials/examples/simple-chatbot.md
  # Architecture without clear feature owner
  - docs/architecture/extending-composition-patterns.md

features:
  # security-audit is single-sourced (status: manual) from
  # content/features/security-audit.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/security-audit/*.md. The entry is retained —
  # WITHOUT `files:` and marked `status: manual` — so topic resolution
  # still routes here while staleness/maintenance never runs
  # `attune-author generate security-audit --all-kinds` to overwrite
  # the projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # The cross-cutting docs/how-to/security-architecture.md remains
  # hand-authored (it is not a projected feature page). Note: the prior
  # `files:` glob conflated `src/attune/monitoring/**` (a separate
  # subsystem) with the audit workflow; the projected docs scope to the
  # workflow only. See docs/specs/help-docs-single-source/.
  security-audit:
    description: Scan code for security vulnerabilities — eval/exec, path traversal, hardcoded secrets, injection risks
    tags: [security, audit, owasp, scanning, cve]
    status: manual

  # elicitation-forms is single-sourced (status: manual) from
  # content/features/elicitation-forms.md via attune_author.projector
  # (scripts/project_features.py), which owns
  # .help/templates/elicitation-forms/*.md. Entry retained WITHOUT
  # `files:` and marked `status: manual` so topic resolution routes here
  # while maintenance never overwrites the projected content with LLM
  # output. See docs/specs/help-docs-single-source/ and
  # docs/specs/elicitation-form-surface/.
  elicitation-forms:
    description: Dynamic forms and the agent-to-user communication grammar — intake, decision, pushback, progress
    tags: [elicitation, forms, communication, ux, widget]
    status: manual

  # code-quality is single-sourced (status: manual) from
  # content/features/code-quality.md via
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/code-quality/*.md. The entry is retained — WITHOUT
  # `files:` and marked `status: manual` — so topic resolution still
  # routes here while staleness/maintenance never runs
  # `attune-author generate code-quality --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  code-quality:
    description: Review code for style issues, likely bugs, and structural problems
    tags: [review, quality, bugs, lint]
    status: manual

  # smart-test is single-sourced (status: manual) from
  # content/features/smart-test.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/smart-test/*.md. The entry is retained — WITHOUT
  # `files:` and marked `status: manual` — so topic resolution still
  # routes here while staleness/maintenance never runs
  # `attune-author generate smart-test --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  smart-test:
    description: Find untested code and generate pytest tests with edge cases
    tags: [tests, coverage, generation]
    status: manual

  # doc-gen is single-sourced (status: manual) from
  # content/features/doc-gen.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/doc-gen/*.md. The entry is retained — WITHOUT
  # `files:` and marked `status: manual` — so topic resolution still
  # routes here while staleness/maintenance never runs
  # `attune-author generate doc-gen --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  doc-gen:
    description: Generate documentation from source code — docstrings, README sections, API references
    tags: [docs, documentation, generation]
    status: manual

  # bug-predict is single-sourced (status: manual) from
  # content/features/bug-predict.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/bug-predict/*.md. The entry is retained — WITHOUT
  # `files:` and marked `status: manual` — so topic resolution still
  # routes here while staleness/maintenance never runs
  # `attune-author generate bug-predict --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  bug-predict:
    description: Predict likely bug locations based on code patterns and complexity
    tags: [bugs, prediction, scanning, race-condition]
    status: manual

  # release-notes is single-sourced (status: manual) from
  # content/features/release-notes.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/release-notes/*.md. The entry is marked
  # `status: manual` — WITHOUT `files:` — so topic resolution routes
  # here while staleness/maintenance never runs
  # `attune-author generate release-notes --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  release-notes:
    description: Draft release notes + an LLM go/no-go readiness advisory (Agent SDK)
    tags: [release, changelog, advisory]
    status: manual

  # release-prep is single-sourced (status: manual) from
  # content/features/release-prep.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/release-prep/*.md. The entry is marked
  # `status: manual` — WITHOUT `files:` — so topic resolution routes
  # here while staleness/maintenance never runs
  # `attune-author generate release-prep --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  release-prep:
    description: Pre-release quality gate — health checks, security audit, changelog, version bumps
    tags: [release, publishing, quality]
    status: manual

  # ops-dashboard is single-sourced (status: manual) from
  # content/features/ops-dashboard.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/ops-dashboard/*.md. Marked `status: manual` WITHOUT
  # `files:` so topic resolution routes here while staleness/maintenance
  # never overwrites the projected content with LLM output (DD5,
  # help-docs-single-source). The frozen faq.md stays on disk until the
  # four-channel FAQ Generator (D6/D7) replaces it. Scope is the runner
  # CORE (src/attune/ops/**) — cost/telemetry/help shown on the
  # dashboard (ops.anthropic_cost / ops.data / ops.help_data) are
  # adjacent READ-ONLY surfaces owned by their own features, not this
  # one. Description/tags unchanged to preserve topic resolution.
  ops-dashboard:
    description: Local operations dashboard — workflow runner with per-feature scope picker, persisted run history, clickable workflow chaining, and live SSE log streaming
    tags: [ops, dashboard, runner, workflows, scope-picker, persistence, sse]
    status: manual

  # refactor-plan is single-sourced (status: manual) from
  # content/features/refactor-plan.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/refactor-plan/*.md. The entry is retained — WITHOUT
  # `files:` and marked `status: manual` — so topic resolution still
  # routes here while staleness/maintenance never runs
  # `attune-author generate refactor-plan --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  refactor-plan:
    description: Detect code smells and generate a prioritized refactoring roadmap
    tags: [refactor, tech-debt, complexity]
    status: manual

  # deep-review is single-sourced (status: manual) from
  # content/features/deep-review.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/deep-review/*.md. The entry is retained — WITHOUT
  # `files:` and marked `status: manual` — so topic resolution still
  # routes here while staleness/maintenance never runs
  # `attune-author generate deep-review --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-23). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  deep-review:
    description: Multi-pass deep code review — security, quality, and test gap analysis
    tags: [review, security, quality, tests, comprehensive-review]
    status: manual

  fix-test:
    description: Auto-diagnose and fix failing tests — identifies root causes and applies fixes
    tags: [tests, debugging, fixes]
    status: manual

  # mcp-server is single-sourced (status: manual) from
  # content/features/mcp-server.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/mcp-server/*.md. The entry is marked `status: manual`
  # — WITHOUT `files:` — so topic resolution routes here while
  # staleness/maintenance never runs `attune-author generate mcp-server
  # --all-kinds` to overwrite the projected content with LLM output
  # (DD5, help-docs-single-source decision, 2026-06-23). The retained
  # faq.md stays on disk frozen until the four-channel FAQ Generator
  # (D6/D7) replaces that path. Description and tags unchanged to
  # preserve golden-query resolution (mc-001 "mcp-server"; mc-002 "mcp").
  # See docs/specs/help-docs-single-source/.
  mcp-server:
    description: Model Context Protocol server and tool handlers
    tags: [mcp, tools, server]
    status: manual

  # help-system is single-sourced (status: manual) from
  # content/features/help-system.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/help-system/*.md. Marked `status: manual` WITHOUT
  # `files:`/`doc_paths:` so topic resolution routes here while
  # staleness/maintenance never overwrites the projected content with
  # LLM output (DD5, help-docs-single-source). The frozen faq.md stays
  # on disk until the four-channel FAQ Generator (D6/D7) replaces it.
  # Scope is the help ENGINE (src/attune/help/**) — NOT the rollout
  # tooling (scripts/project_features.py, content/) or the ops help tab
  # (attune.ops.help_data). Description/tags unchanged to preserve
  # golden queries hs-001 "help-system", hs-002 "templates".
  help-system:
    description: Progressive-depth help engine and template management
    tags: [help, templates, docs]
    status: manual

  # memory is single-sourced (status: manual) from
  # content/features/memory.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/memory/*.md. The entry is marked `status: manual`
  # — WITHOUT `files:` — so topic resolution routes here while
  # staleness/maintenance never runs `attune-author generate memory
  # --all-kinds` to overwrite the projected content with LLM output
  # (DD5, help-docs-single-source decision, 2026-06-23). The retained
  # faq.md stays on disk frozen until the four-channel FAQ Generator
  # (D6/D7) replaces that path. The cross-cutting hand-authored
  # docs/how-to/memory-graph.md and docs/how-to/unified-memory-system.md
  # remain as-is (they are not projected feature pages). Description and
  # tags are unchanged to preserve golden-query resolution (mem-002
  # "storage"; rg-003 relies on memory using "lookup" not "retrieval").
  # See docs/specs/help-docs-single-source/.
  memory:
    description: Memory subsystem — storage, lookup, and security
    tags: [memory, storage]
    status: manual

  # agents is single-sourced (status: manual) from
  # content/features/agents.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/agents/*.md. The entry is marked `status: manual`
  # — WITHOUT `files:` — so topic resolution routes here while
  # staleness/maintenance never runs `attune-author generate agents
  # --all-kinds` to overwrite the projected content with LLM output
  # (DD5, help-docs-single-source decision, 2026-06-23). The retained
  # faq.md stays on disk frozen until the four-channel FAQ Generator
  # (D6/D7) replaces that path. SCOPE: this feature is the Universal
  # Agent Factory (src/attune/agent_factory/); the prior `files:` glob
  # conflated the release agent TEAM (src/attune/agents/release/, now
  # under release-prep) and its state/recovery store
  # (src/attune/agents/state/). Description updated + "release" tag
  # dropped to match; the old hand-authored agent-factory docs are
  # superseded by the projected agents pages. "agents"/"ai" tags
  # preserved for golden-query resolution (ag-001/ag-002).
  # See docs/specs/help-docs-single-source/.
  agents:
    description: Universal Agent Factory — create, run, and orchestrate AI agents across frameworks
    tags: [agents, ai]
    status: manual

  cli:
    # Single-sourced via content/features/cli.md (projector).
    # Scope = cli_minimal.py (argparse main() + cmd_* handlers) +
    # cli_router.py (route_user_input [async] / is_slash_command /
    # SmartRouter / HybridRouter) + cli_commands/**.
    description: Command-line interface and routing
    tags: [cli, commands]
    status: manual

  # models is single-sourced (status: manual) from
  # content/features/models.md and rendered by attune_author.projector
  # (scripts/project_features.py), which owns .help/templates/models/*.md.
  # The entry is retained — WITHOUT `files:` and marked `status: manual`
  # — so topic resolution (resolve_topic scores name/description/tags)
  # still routes here, while staleness/maintenance never regenerates it
  # (DD5 / D9, help-docs-single-source, 2026-06-21). The retained faq.md
  # stays on disk frozen until the FAQ Generator (D6/D7) ships; the
  # cross-cutting docs/how-to/smart-router.md and
  # docs/reference/llm-toolkit.md remain hand-authored. See
  # docs/specs/help-docs-single-source/.
  models:
    description: LLM authentication, provider routing, and tier management
    tags: [models, auth, llm]
    status: manual

  orchestration:
    # Single-sourced via content/features/orchestration.md (projector).
    # Scope = the attune.orchestration package (AgentTemplate registry +
    # ExecutionStrategy/get_strategy) plus the runnable team primitive
    # attune.agents.team.AgentTeam (WorkflowAgent/GateSpec/TeamReport).
    # The dead MetaOrchestrator/DynamicTeam/WorkflowComposer engine was
    # removed in #1096; the docs were cleaned in the orchestration-doc-
    # fiction-cleanup spec. Overlaps the agents feature (agent factory)
    # — distinct: this is the templates + strategies + team-runner layer,
    # not the factory.
    description: Agent-template registry, execution strategies, and parallel agent teams with quality gates
    tags: [orchestration, teams]
    status: manual

  telemetry:
    # Single-sourced via content/features/telemetry.md (projector).
    # Scope = the attune.telemetry package: UsageTracker (usage ledger)
    # + FeedbackLoop (model-tier feedback). The package ALSO exports
    # agent-coordination classes (EventStreamer / HeartbeatCoordinator /
    # ApprovalGate) that overlap the orchestration feature — documented
    # here as a secondary group, NOT duplicated under orchestration.
    description: Usage tracking and feedback loops
    tags: [telemetry, metrics]
    status: manual

  # plugin is single-sourced (status: manual) from
  # content/features/plugin.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/plugin/*.md. The entry is retained — WITHOUT
  # `files:`/`arch_path:` and marked `status: manual` — so topic
  # resolution still routes here while staleness/maintenance never runs
  # the LLM generator to overwrite the projected content (DD5,
  # help-docs-single-source decision, 2026-06-24). The retained faq.md
  # stays on disk frozen as a manual file.
  plugin:
    description: Claude Code plugin — skills, hooks, commands, and MCP config
    tags: [plugin, claude-code]
    status: manual

  # spec-engine is single-sourced (status: manual) from
  # content/features/spec-engine.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/spec-engine/*.md. The entry is retained — WITHOUT
  # `files:` and marked `status: manual` — so topic resolution still
  # routes here while staleness/maintenance never runs
  # `attune-author generate spec-engine --all-kinds` to overwrite the
  # projected content with LLM output (DD5, help-docs-single-source
  # decision, 2026-06-21). The retained faq.md stays on disk frozen
  # until the four-channel FAQ Generator (D6/D7) replaces that path.
  # See docs/specs/help-docs-single-source/.
  spec-engine:
    description: Spec-driven development with approval loops
    tags: [spec, planning]
    status: manual

  # wizards is single-sourced (status: manual) from
  # content/features/wizards.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/wizards/*.md. The entry is marked `status: manual`
  # — WITHOUT `files:` — so topic resolution routes here while
  # staleness/maintenance never runs `attune-author generate wizards
  # --all-kinds` to overwrite the projected content with LLM output
  # (DD5, help-docs-single-source decision, 2026-06-23). The retained
  # faq.md stays on disk frozen until the four-channel FAQ Generator
  # (D6/D7) replaces that path. The old hand-authored
  # docs/reference/wizards.md is now the projected reference page.
  # Description and tags are unchanged to preserve golden-query
  # resolution (wz-002 "interactive"). See docs/specs/help-docs-single-source/.
  wizards:
    description: Multi-step guided interactive workflows
    tags: [wizards, interactive]
    status: manual

  configuration:
    # Single-sourced via content/features/configuration.md (projector).
    # Scope = the attune.config package, which spans FOUR layers with
    # overlapping names: the unified tree (UnifiedConfig + sections +
    # config.loader + validation), agent config (UnifiedAgentConfig),
    # the XML/empathy layer (EmpathyXMLConfig via get_config/set_config),
    # and the legacy AttuneConfig (== EmpathyConfig) dataclass loaded by
    # load_config. The doc distinguishes all four (the prior generated
    # concept doc mis-attributed config.loader symbols to config.unified).
    description: Project configuration and settings management
    tags: [config, settings]
    status: manual

  # rag-grounding is single-sourced (status: manual) from
  # content/features/rag-grounding.md and rendered by
  # attune_author.projector (scripts/project_features.py), which owns
  # .help/templates/rag-grounding/*.md. The entry is marked
  # `status: manual` — WITHOUT `files:` — so topic resolution routes
  # here while staleness/maintenance never runs `attune-author generate
  # rag-grounding --all-kinds` to overwrite the projected content with
  # LLM output (DD5, help-docs-single-source decision, 2026-06-23). The
  # retained faq.md stays on disk frozen until the four-channel FAQ
  # Generator (D6/D7) replaces that path. Description and tags are
  # unchanged to preserve golden-query resolution (rg-002 "grounding";
  # rg-003 relies on "retrieval"/"retrieves" routing here, not to memory).
  # The feature/help name is rag-grounding; the CLI workflow slug is
  # rag-code-gen. See docs/specs/help-docs-single-source/.
  rag-grounding:
    description: RAG-grounded code generation — retrieves attune-help context via attune-rag, feeds citation-forced prompts to Claude, emits answers with provenance
    tags: [rag, retrieval, grounding, faithfulness, citation]
    status: manual

  resilience:
    # Single-sourced via content/features/resilience.md (projector).
    # Scope = the attune.resilience package: retry / circuit_breaker /
    # timeout / fallback / health, each a decorator + underlying class.
    # Adversarial review caught HealthCheck.register being a decorator
    # (not register(name, fn)) — fixed in the master.
    description: Fault-tolerance primitives — circuit breakers, retries, timeouts, fallbacks, health checks
    tags: [resilience, fault-tolerance, retry, circuit-breaker, reliability]
    status: manual

  hooks:
    # Single-sourced via content/features/hooks.md (projector).
    # Scope = the attune.hooks package: HookEvent + HookRegistry
    # (in-process) + HookExecutor/HookConfig/HookDefinition (declarative)
    # + the bundled scripts/ hooks. Key gotcha documented: handlers are
    # called handler(**context) (kwargs-unpacked), not handler(context).
    description: Hook system — pre/post-tool events, webhooks, and hook executor
    tags: [hooks, webhooks, events, automation]
    status: manual
